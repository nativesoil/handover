# Compatibility

<!--
  Generated by scripts/generate-compatibility.mjs from
  compatibility/environments.yaml and compatibility/integrations.yaml.
  Do not edit this file by hand: edit the source data and rerun
  `node scripts/generate-compatibility.mjs`. The compatibility workflow
  fails when this file drifts from the source.
-->

What this format and its implementations can do, how mature each path is,
and how each claim was proven. The machine-readable source of truth lives in
[compatibility/environments.yaml](../compatibility/environments.yaml) and
[compatibility/integrations.yaml](../compatibility/integrations.yaml); this
page, the block in the README and
[compatibility/readme-snippet.md](../compatibility/readme-snippet.md) are all
generated from those two files, and CI fails when any of the three drifts.

### What the columns mean

**Maturity** is the primary axis, because a reader wants to know which
features exist and which of them work:

- **works**: every capability listed has been exercised end to end.
- **partial**: some of the listed capability works, the rest is not exercised.
- **unproven**: nothing exercised. The claim rests on a surface, not on a run.
- **planned**: nothing exercised and nothing claimed. Intent only.

**Evidence** is a second axis and never collapses into the first. Saying
something works when only we have seen it is not the same claim as saying it
works and here is how you check:

- **reproducible-in-repo**: an artefact in this repository reproduces it. CI,
  the conformance suite, or a runnable example anybody can run.
- **open-integration-observed**: a real run against an openly available
  integration, recorded in a report committed here. Repeatable by hand, not
  automated.
- **hosted-integration-observed**: a real run that went through a hosted
  service, recorded in a report committed here.
- **maintainer-attested**: the maintainers ran it and logged it internally.
  There is no artefact a reader can inspect.
- **source-reviewed**: read from source, specification or the vendor's
  documentation, never executed.
- **planned**: nothing has been run. The row states an intention, and carries
  no evidence.

An internal log with no publicly reproducible artefact is a maintainer
attestation, not a demonstration. Ten of the seventeen client rows carry that
label: they were exercised through the hosted connector, and a reader outside
the maintainers cannot check any of them. No row currently qualifies for
hosted-integration-observed, because no hosted run has a report committed
here.

**Where a claim comes from** is a third axis, on the client rows. What a
client CAN do and what WE have shown are different statements, and each row
says whose statement it is: the vendor's own documentation, cited in the row,
or this repository's record. Vendor documentation supports what a surface can
attach. It never supports a run, so a row resting on it stays unproven with
its evidence read from documentation, and no handover is ever claimed to have
travelled anywhere on a vendor's word.

**Can you check it yourself** is the Evidence column said in the words it
means for a reader, and it is the column the README keeps. **Evidence** holds
the label itself, because the label is what a row is audited against.

The page is three tables because a reader arrives with one of three
questions. Client surfaces answer somebody picking a client, one row per
surface and claim, because a product's desktop app and its web page have
different integration properties and one row for both hides the difference.
This repository's own rows answer somebody deciding what to run, described by
who each is for. The evidence table carries how every row was proven, so
neither of the first two leads with audit vocabulary.

## Client surfaces

One row per client surface and claim. A product's desktop app, its web page,
its CLI and its IDE form are different surfaces, and they get different rows
because what is true of one is routinely false of another.

| Client               | Surface         | Maturity | Capability                        | Reached through                                                                      | The claim rests on                                                                                                                     |
| -------------------- | --------------- | -------- | --------------------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| Claude Code          | CLI             | works    | save, load, list                  | a local MCP server, started by the client                                            | a session report committed here                                                                                                        |
| Claude Code          | CLI             | works    | save, load, list                  | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| ChatGPT              | web and iOS     | works    | save, load, interactive UI cards  | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| ChatGPT              | desktop app     | unproven | attach local and HTTP MCP servers | a local MCP server, started by the client; an MCP server you run yourself, over HTTP | [the vendor's documentation](https://learn.chatgpt.com/docs/extend/mcp)                                                                |
| Claude               | web and iOS     | works    | save, load, list, info, UI cards  | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| Claude               | desktop app     | unproven | attach local MCP servers          | a local MCP server, started by the client                                            | [the vendor's documentation](https://support.claude.com/en/articles/10949351-getting-started-with-local-mcp-servers-on-claude-desktop) |
| Codex                | CLI             | works    | MCP setup, handover flow          | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| Codex                | CLI             | works    | save, load                        | a local MCP server, started by the client                                            | a session report committed here                                                                                                        |
| Grok                 | web             | partial  | load                              | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| Cursor               | IDE             | works    | project handover flow             | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| Cursor               | IDE             | unproven | attach local MCP servers          | a local MCP server, started by the client                                            | [the vendor's documentation](https://cursor.com/docs/mcp)                                                                              |
| VS Code              | IDE             | works    | project handover flow             | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| VS Code              | IDE             | unproven | attach local MCP servers          | a local MCP server, started by the client                                            | [the vendor's documentation](https://code.visualstudio.com/docs/copilot/chat/mcp-servers)                                              |
| Lovable              | web app builder | works    | project handover flow             | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| v0                   | web app builder | works    | project handover flow             | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| Base44               | web app builder | partial  | connection and authorization      | the hosted Native Soil connector                                                     | our internal log                                                                                                                       |
| Any other MCP client | varies          | unproven | none run                          | a local MCP server, started by the client                                            | a reading of the code here                                                                                                             |

Model families exercised in the runs behind these rows: OpenAI, Anthropic
and xAI.

### What is not in this repository

Ten of the seventeen client rows above record runs through the hosted Native
Soil connector: a separate service with accounts, whose code is not here and
which nothing here calls. Installing this repository gives you none of those
runs. The clients are Claude Code, ChatGPT, Claude, Codex, Grok, Cursor, VS
Code, Lovable, v0 and Base44.

ChatGPT (web and iOS), Claude (web and iOS), Grok (web), Lovable (web app
builder), v0 (web app builder) and Base44 (web app builder) are a page in a
browser or an app on a phone. Nothing that runs elsewhere can start a process
on your machine, so those surfaces have no local path: the hosted connector,
or plain copy and paste through the recipe and the restore prompt, is how they
reach the format.

Every hosted row rests on a maintainer log nobody outside this project can
inspect. No hosted run has a report committed here, so the hosted rows stop at
our word.

### Documented by the vendor, not yet run here

ChatGPT (desktop app), Claude (desktop app), Cursor (IDE) and VS Code (IDE)
run on your machine, and each vendor's own documentation covers attaching
local MCP servers; every such row cites the page it rests on. That is the
vendor's statement about the surface, read from documentation, never a run: no
save or load of this format is recorded through those rows.

Claude Code (CLI) and Codex (CLI) have gone further on the local path: a
recorded session against the local server this repository ships, with the
session report committed here.

## This repository's own surfaces

Eight surfaces, every one of them shipped here and run on your own machine,
from a checkout of this repository. No account, and nothing over the network.

| Surface                        | Who it is for                                                                    | What works                        | Can you check it yourself     |
| ------------------------------ | -------------------------------------------------------------------------------- | --------------------------------- | ----------------------------- |
| Local CLI                      | You, at a terminal, driving the round trip by hand                               | save, load, list, validate, check | yes, an artefact here does it |
| Local MCP server               | Any MCP client on this machine, so the model saves and loads itself              | save, load, list                  | yes, an artefact here does it |
| Self-hostable server (preview) | An operator running one server for a team, on hardware they choose               | save, load, list, shared projects | yes, an artefact here does it |
| TypeScript SDK                 | Developers building the format into their own tools, in TypeScript or JavaScript | validate, save, load, check       | yes, an artefact here does it |
| Python SDK                     | Developers building the format into their own tools, in Python                   | validate, save, load, check       | yes, an artefact here does it |
| Go SDK                         | Developers building the format into their own tools, in Go                       | validate, save, load, check       | yes, an artefact here does it |
| JVM SDK                        | Developers building the format into their own tools, in Kotlin or Java           | validate, save, load, check       | yes, an artefact here does it |
| .NET SDK                       | Developers building the format into their own tools, in C#                       | validate, save, load, check       | yes, an artefact here does it |

What each capability word claims:

- **save** stores a handover. Every save is validated against the schema and
  scanned for credential-shaped values and private paths, and a document that
  fails either gate is not stored. The scan is pattern-based: a net, not a
  guarantee.
- **load** renders a stored handover back as the restore prompt a new session
  continues from.
- **list** shows what the store holds.
- **validate** answers whether a file is a correct handover, with the exact
  path of each fault and an exit code a script can gate on.
- **check** grades content quality against the published rules: the document
  is checked and graded, and the grade never blocks a save.
- **shared projects** means one project store that several members save to and
  load from.

## The evidence, row by row

Every row above, with how it was proven, when, and what that proof does not
cover.

| Row                                         | Evidence                  | Can you check it yourself        | Reproducible here | How it connects                                                                      | Evidence date | Limitation                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------- | ------------------------- | -------------------------------- | ----------------- | ------------------------------------------------------------------------------------ | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Claude Code (CLI; local MCP)                | open-integration-observed | a report of one real run is here | no                | a local MCP server, started by the client                                            | 2026-07-24    | One recorded session, on macOS, repeatable by hand; not an automated check.                                                                                                                                                                                                                                                                 |
| Claude Code (CLI; hosted)                   | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-07       | Run only through the hosted connector, with no inspectable artefact.                                                                                                                                                                                                                                                                        |
| ChatGPT (web and iOS; hosted)               | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-07       | Run only through the hosted connector, with no inspectable artefact.                                                                                                                                                                                                                                                                        |
| ChatGPT (desktop app; local MCP + HTTP MCP) | source-reviewed           | no, a reading, never a run       | no                | a local MCP server, started by the client; an MCP server you run yourself, over HTTP | 2026-07-30    | The vendor documents the surface; no run through it is recorded in this repository. The HTTP path does not pair with the self-hostable server as built, because this client's connector flow requires an OAuth authorization server and that server is deliberately bearer-token only; the hosted row above is the route that remains.      |
| Claude (web and iOS; hosted)                | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-07       | Run only through the hosted connector, with no inspectable artefact.                                                                                                                                                                                                                                                                        |
| Claude (desktop app; local MCP)             | source-reviewed           | no, a reading, never a run       | no                | a local MCP server, started by the client                                            | 2026-07-30    | The vendor documents the surface; no run through it is recorded in this repository. The self-hostable server is out of this surface's reach as built, because this client's remote connector flow requires an OAuth authorization server and that server is deliberately bearer-token only; the hosted row above is the route that remains. |
| Codex (CLI; hosted)                         | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-07       | Run only through the hosted connector, with no inspectable artefact.                                                                                                                                                                                                                                                                        |
| Codex (CLI; local MCP)                      | open-integration-observed | a report of one real run is here | no                | a local MCP server, started by the client                                            | 2026-07-30    | One recorded session, on macOS, repeatable by hand; not an automated check.                                                                                                                                                                                                                                                                 |
| Grok (web; hosted)                          | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-06       | A load was run; a save was not. Hosted connector only, no inspectable artefact.                                                                                                                                                                                                                                                             |
| Cursor (IDE; hosted)                        | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-06       | Run only through the hosted connector, with no inspectable artefact.                                                                                                                                                                                                                                                                        |
| Cursor (IDE; local MCP)                     | source-reviewed           | no, a reading, never a run       | no                | a local MCP server, started by the client                                            | 2026-07-30    | The vendor documents the surface; no run through it is recorded in this repository.                                                                                                                                                                                                                                                         |
| VS Code (IDE; hosted)                       | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-06       | Run only through the hosted connector, with no inspectable artefact.                                                                                                                                                                                                                                                                        |
| VS Code (IDE; local MCP)                    | source-reviewed           | no, a reading, never a run       | no                | a local MCP server, started by the client                                            | 2026-07-30    | The vendor documents the surface; no run through it is recorded in this repository.                                                                                                                                                                                                                                                         |
| Lovable (web app builder; hosted)           | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-06       | Run only through the hosted connector, with no inspectable artefact.                                                                                                                                                                                                                                                                        |
| v0 (web app builder; hosted)                | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-06       | Run only through the hosted connector, with no inspectable artefact.                                                                                                                                                                                                                                                                        |
| Base44 (web app builder; hosted)            | maintainer-attested       | no, our word only                | no                | the hosted Native Soil connector                                                     | 2026-06       | Connection and authorization only; no handover flow has run there.                                                                                                                                                                                                                                                                          |
| Any other MCP client (varies; local MCP)    | source-reviewed           | no, a reading, never a run       | no                | a local MCP server, started by the client                                            | 2026-07-24    | No third-party client beyond those above has been run. The row is a reading, not a result.                                                                                                                                                                                                                                                  |
| Local CLI                                   | reproducible-in-repo      | yes, an artefact here does it    | yes               | the command-line tool                                                                | 2026-07-24    | The by-hand stranger test ran on macOS; CI covers three operating systems.                                                                                                                                                                                                                                                                  |
| Local MCP server                            | reproducible-in-repo      | yes, an artefact here does it    | yes               | the local server, over stdio                                                         | 2026-07-24    | The headless client session ran on macOS.                                                                                                                                                                                                                                                                                                   |
| Self-hostable server (preview)              | reproducible-in-repo      | yes, an artefact here does it    | yes               | the self-hostable server, over HTTP                                                  | 2026-07-27    | A preview. Single node, tokens the operator hands out, not hardened for multi-tenant use. Its MCP endpoint is exercised only by this repository's own suite; no external client has been run against it.                                                                                                                                    |
| TypeScript SDK                              | reproducible-in-repo      | yes, an artefact here does it    | yes               | as a library, in your own code                                                       | 2026-07-30    | Conformance is a fixture suite; it says nothing about content quality.                                                                                                                                                                                                                                                                      |
| Python SDK                                  | reproducible-in-repo      | yes, an artefact here does it    | yes               | as a library, in your own code                                                       | 2026-07-30    | Conformance is a fixture suite; it says nothing about content quality.                                                                                                                                                                                                                                                                      |
| Go SDK                                      | reproducible-in-repo      | yes, an artefact here does it    | yes               | as a library, in your own code                                                       | 2026-07-30    | Conformance is a fixture suite; it says nothing about content quality.                                                                                                                                                                                                                                                                      |
| JVM SDK                                     | reproducible-in-repo      | yes, an artefact here does it    | yes               | as a library, in your own code                                                       | 2026-07-30    | CI runs the JVM suite on ubuntu-latest only.                                                                                                                                                                                                                                                                                                |
| .NET SDK                                    | reproducible-in-repo      | yes, an artefact here does it    | yes               | as a library, in your own code                                                       | 2026-07-30    | CI runs the .NET suite on ubuntu-latest only.                                                                                                                                                                                                                                                                                               |

### Evidence sources and limitations

- **Claude Code (CLI; local MCP)** (open-integration-observed): Headless Claude Code session on 2026-07-24 driving soil_list, soil_save and soil_load against the local server from packages/mcp, on a scratch store: [session report](../compatibility/reports/local-mcp-claude-code.md). Limitation: One recorded session, on macOS, repeatable by hand; not an automated check.
- **Claude Code (CLI; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07, from daily use on active development projects. Nothing in this repository reproduces it. Limitation: Run only through the hosted connector, with no inspectable artefact.
- **ChatGPT (web and iOS; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: Run only through the hosted connector, with no inspectable artefact.
- **ChatGPT (desktop app; local MCP + HTTP MCP)** (source-reviewed): [OpenAI's MCP page](https://learn.chatgpt.com/docs/extend/mcp) documents the ChatGPT desktop app attaching STDIO servers, run as a local process, and Streamable HTTP servers reached at an address. Read on 2026-07-30; no run through this surface is recorded here. Limitation: The vendor documents the surface; no run through it is recorded in this repository. The HTTP path does not pair with the self-hostable server as built, because this client's connector flow requires an OAuth authorization server and that server is deliberately bearer-token only; the hosted row above is the route that remains.
- **Claude (web and iOS; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: Run only through the hosted connector, with no inspectable artefact.
- **Claude (desktop app; local MCP)** (source-reviewed): [Anthropic's page on local MCP servers in Claude Desktop](https://support.claude.com/en/articles/10949351-getting-started-with-local-mcp-servers-on-claude-desktop) documents installing local MCP servers on the desktop app, packaged as desktop extensions. Read on 2026-07-30; no run through this surface is recorded here. Limitation: The vendor documents the surface; no run through it is recorded in this repository. The self-hostable server is out of this surface's reach as built, because this client's remote connector flow requires an OAuth authorization server and that server is deliberately bearer-token only; the hosted row above is the route that remains.
- **Codex (CLI; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: Run only through the hosted connector, with no inspectable artefact.
- **Codex (CLI; local MCP)** (open-integration-observed): Headless Codex CLI session on 2026-07-30 driving soil_load and soil_save against the local server from packages/mcp, on a scratch store, as one leg of [the client-switch walkthrough](../docs/switch-clients.md): it loaded the handover committed as [saved-by-claude-code.json](../docs/switch-clients/saved-by-claude-code.json) and stored [saved-by-codex.json](../docs/switch-clients/saved-by-codex.json), which is held byte for byte to the receipt shown on that page. Limitation: One recorded session, on macOS, repeatable by hand; not an automated check.
- **Grok (web; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: A load was run; a save was not. Hosted connector only, no inspectable artefact.
- **Cursor (IDE; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: Run only through the hosted connector, with no inspectable artefact.
- **Cursor (IDE; local MCP)** (source-reviewed): [Cursor's MCP page](https://cursor.com/docs/mcp) documents stdio servers, started by the editor and configured in mcp.json. Read on 2026-07-30; no run through this surface's local path is recorded here. Limitation: The vendor documents the surface; no run through it is recorded in this repository.
- **VS Code (IDE; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: Run only through the hosted connector, with no inspectable artefact.
- **VS Code (IDE; local MCP)** (source-reviewed): [VS Code's MCP page](https://code.visualstudio.com/docs/copilot/chat/mcp-servers) documents local stdio servers configured in mcp.json. Read on 2026-07-30; no run through this surface's local path is recorded here. Limitation: The vendor documents the surface; no run through it is recorded in this repository.
- **Lovable (web app builder; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: Run only through the hosted connector, with no inspectable artefact.
- **v0 (web app builder; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: Run only through the hosted connector, with no inspectable artefact.
- **Base44 (web app builder; hosted)** (maintainer-attested): Maintainers' internal test log, 2026-06/07. Nothing in this repository reproduces it. Limitation: Connection and authorization only; no handover flow has run there.
- **Any other MCP client (varies; local MCP)** (source-reviewed): The local server in [packages/mcp](../packages/mcp) speaks MCP over stdio the way the specification defines it, read from its source and from that specification. This row is the local path only: what a client reaches through the hosted connector instead is on that client's own rows. Limitation: No third-party client beyond those above has been run. The row is a reading, not a result.
- **Local CLI** (reproducible-in-repo): The CLI suite runs on every push in [the CI workflow](../.github/workflows/ci.yml), covering save, load, list, validate, check and the fail-closed refusal paths. A fresh-clone stranger test on 2026-07-24 ran the same round trip by hand. That run has no published artefact and is not what this row rests on. Limitation: The by-hand stranger test ran on macOS; CI covers three operating systems.
- **Local MCP server** (reproducible-in-repo): Protocol round trip in the root test suite, run by [the CI workflow](../.github/workflows/ci.yml) on every push. Headless Claude Code session on 2026-07-24 driving soil_list, soil_save and soil_load against a scratch store: [session report](../compatibility/reports/local-mcp-claude-code.md). Limitation: The headless client session ran on macOS.
- **Self-hostable server (preview)** (reproducible-in-repo): The suite in [packages/server](../packages/server) drives the running server over real HTTP on every push, through [the CI workflow](../.github/workflows/ci.yml): bearer-token refusals, a personal save, list and load, a shared-project save read back by a second member, a non-member getting 404 on every project route, and the fail-closed refusal that stores nothing. The same suite drives the server's MCP endpoint over HTTP through the three tools. Not covered: no client outside this repository has been pointed at it, and the operator setup in [docs/server.md](../docs/server.md) is run by hand rather than checked. Limitation: A preview. Single node, tokens the operator hands out, not hardened for multi-tenant use. Its MCP endpoint is exercised only by this repository's own suite; no external client has been run against it.
- **TypeScript SDK** (reproducible-in-repo): The official source implementation, and the one the CLI, the local MCP server and the self-hostable server are built on. Its suite and the TypeScript conformance runner run in [the CI workflow](../.github/workflows/ci.yml) on ubuntu, macos and windows: document validation and the fail-closed writer pipeline, held to the shared fixtures. Load renders the restore prompt, and this SDK is the reference for its bytes: [its restore suite](../packages/sdk-ts/src/restore.test.ts) and the adversarial restore fixtures in the conformance suite pin the output in [the CI workflow](../.github/workflows/ci.yml), and the other four SDKs' restore goldens are generated from this SDK's built output. The cross-language store harness in the same workflow drives one writer per implementation against one store, and each suite holds the deterministic check report and card byte-identical to the reference over a shared corpus. Limitation: Conformance is a fixture suite; it says nothing about content quality.
- **Python SDK** (reproducible-in-repo): Conformance runner in [the CI workflow](../.github/workflows/ci.yml) on ubuntu, macos and windows: document validation and the fail-closed writer pipeline, held to the shared fixtures. Load renders the restore prompt: [the parity suite](../packages/sdk-py/tests/test_parity.py) holds it byte for byte to the built TypeScript reference, over a thirty-document corpus of hostile working-style inputs, on every push in [the CI workflow](../.github/workflows/ci.yml). The cross-language store harness in the same workflow drives one writer per implementation against one store, and each suite holds the deterministic check report and card byte-identical to the reference over a shared corpus. Limitation: Conformance is a fixture suite; it says nothing about content quality.
- **Go SDK** (reproducible-in-repo): Conformance runner in [the CI workflow](../.github/workflows/ci.yml) on ubuntu, macos and windows: document validation and the fail-closed writer pipeline, held to the shared fixtures. Load renders the restore prompt: the goldens in [packages/sdk-go/testdata](../packages/sdk-go/testdata), generated from the built TypeScript reference, hold it byte for byte in the Go suite on every push in [the CI workflow](../.github/workflows/ci.yml), beside that suite's own hostile working-style cases. The cross-language store harness in the same workflow drives one writer per implementation against one store, and each suite holds the deterministic check report and card byte-identical to the reference over a shared corpus. Limitation: Conformance is a fixture suite; it says nothing about content quality.
- **JVM SDK** (reproducible-in-repo): Conformance runner in [the CI workflow](../.github/workflows/ci.yml): document validation and the fail-closed writer pipeline, held to the shared fixtures. Load renders the restore prompt: the goldens in [the JVM parity resources](../packages/sdk-jvm/src/test/resources/parity), generated from the built TypeScript reference, hold it byte for byte in the JVM suite in [the CI workflow](../.github/workflows/ci.yml), beside that suite's own hostile working-style cases. The cross-language store harness in the same workflow drives one writer per implementation against one store, and each suite holds the deterministic check report and card byte-identical to the reference over a shared corpus. Limitation: CI runs the JVM suite on ubuntu-latest only.
- **.NET SDK** (reproducible-in-repo): Conformance runner in [the CI workflow](../.github/workflows/ci.yml): document validation and the fail-closed writer pipeline, held to the shared fixtures. Load renders the restore prompt: the goldens in [the .NET golden files](../packages/sdk-dotnet/SoilHandover.Tests/golden), regenerated from the built TypeScript reference by [tools/generate-golden.mjs](../packages/sdk-dotnet/tools/generate-golden.mjs), hold it byte for byte in the .NET suite in [the CI workflow](../.github/workflows/ci.yml), beside that suite's own hostile working-style cases. The cross-language store harness in the same workflow drives one writer per implementation against one store, and each suite holds the deterministic check report and card byte-identical to the reference over a shared corpus. The cross-language store harness in the same workflow drives one writer per implementation against one store, and each suite holds the deterministic check report and card byte-identical to the reference over a shared corpus. Limitation: CI runs the .NET suite on ubuntu-latest only.

## Integrations

| Integration                  | Maturity | What                                                                                   | Evidence                  | Reproducible here | Hosted service | Evidence date | Limitation                                                                                                                                                                                                                                                                                                                                |
| ---------------------------- | -------- | -------------------------------------------------------------------------------------- | ------------------------- | ----------------- | -------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| JSON Schema                  | works    | The published document schema in spec/handover.schema.json                             | reproducible-in-repo      | yes               | no             | 2026-07-24    | The schema cannot express the safety rule; the suite covers that separately.                                                                                                                                                                                                                                                              |
| MCP, local stdio server      | works    | The local server in packages/mcp, over stdio                                           | reproducible-in-repo      | yes               | no             | 2026-07-24    | The published client session is one run, not an automated check.                                                                                                                                                                                                                                                                          |
| MCP, hosted remote connector | works    | The hosted Native Soil connector, which is how published chat clients reach the format | maintainer-attested       | no                | yes            | 2026-07       | No artefact in this repository reproduces the hosted path.                                                                                                                                                                                                                                                                                |
| AGENTS.md                    | partial  | Instruction files teaching agents the save and load flow                               | reproducible-in-repo      | yes               | no             | 2026-07-31    | The documented commands run on every test run, and one agent has now been recorded installing from the files and wiring the two clients it configured; that is one agent on one machine, at versions matching this repository's own documented run, and that run carried no save in one live model session followed by a load in another. |
| GitHub Actions               | works    | This repository's own CI                                                               | reproducible-in-repo      | yes               | no             | 2026-07-24    | Proves this repository's own pipeline, nothing about anyone else's.                                                                                                                                                                                                                                                                       |
| LangGraph                    | works    | Exporting checkpointed graph state as a portable handover                              | reproducible-in-repo      | yes               | no             | 2026-07-24    | The handover text is assembled by the script from live state, not written by a model.                                                                                                                                                                                                                                                     |
| Mem0                         | partial  | A bounded project handover beside user and agent memory                                | reproducible-in-repo      | partial           | no             | 2026-07-24    | The Soil side is real; the framework side is a labelled stand-in.                                                                                                                                                                                                                                                                         |
| Letta                        | partial  | A portable handover for a cold agent beside a stateful one                             | reproducible-in-repo      | partial           | no             | 2026-07-24    | The Soil side is real; the framework side is a labelled stand-in.                                                                                                                                                                                                                                                                         |
| buzz                         | works    | The soil MCP server attached to a buzz agent session over ACP                          | open-integration-observed | partial           | no             | 2026-07-24    | The run with a real model is a committed report, not an automated check; CI runs a stand-in.                                                                                                                                                                                                                                              |

### Evidence sources and limitations

- **JSON Schema** (reproducible-in-repo): Cross-checked in [the CI workflow](../.github/workflows/ci.yml) on every push: the conformance runner requires the schema and the SDK validators to agree on every shared fixture. Limitation: The schema cannot express the safety rule; the suite covers that separately.
- **MCP, local stdio server** (reproducible-in-repo): Protocol round trip in the root test suite, run by [the CI workflow](../.github/workflows/ci.yml) on every push. Headless Claude Code session against the local server on 2026-07-24: [session report](../compatibility/reports/local-mcp-claude-code.md). Limitation: The published client session is one run, not an automated check.
- **MCP, hosted remote connector** (maintainer-attested): Maintainers' internal test log, 2026-06/07, covering connector flows in published clients. Nothing in this repository reproduces it. Limitation: No artefact in this repository reproduces the hosted path.
- **AGENTS.md** (reproducible-in-repo): The save and load commands in AGENTS.md and [docs/install-with-an-agent.md](../docs/install-with-an-agent.md) are taken from those files' own fenced blocks and run against the real command-line tool and the real local server by the root test suite, on every run, in [the CI workflow](../.github/workflows/ci.yml). Each step is asserted against the outcome its own prose states, so the files cannot drift from the behaviour they describe. Install run on 2026-07-31 by an agent that had not seen the project before, working only from the public repository at the v0.1.0 tag: it followed the files, wired the local server into two clients, and ran the documented smoke test end to end against an isolated temporary store; [session report](../compatibility/reports/agents-md-codex.md). Not covered: no automated check exercises the client wiring in section 3 of AGENTS.md, and the recorded install run ended at the rendered restore prompt rather than at a model continuing the work from it. Limitation: The documented commands run on every test run, and one agent has now been recorded installing from the files and wiring the two clients it configured; that is one agent on one machine, at versions matching this repository's own documented run, and that run carried no save in one live model session followed by a load in another.
- **GitHub Actions** (reproducible-in-repo): [The CI workflow](../.github/workflows/ci.yml) builds, tests and runs conformance on every push and pull request; green on main. Limitation: Proves this repository's own pipeline, nothing about anyone else's.
- **LangGraph** (reproducible-in-repo): Reproducible keyless example under [examples/langgraph](../examples/langgraph): a real checkpointed StateGraph, the handover derived from live state, checked and loaded by the reference CLI in [the examples workflow](../.github/workflows/examples.yml). Limitation: The handover text is assembled by the script from live state, not written by a model.
- **Mem0** (reproducible-in-repo): Runnable example under [examples/mem0](../examples/mem0): the Soil side is real, the framework side is a clearly labeled stand-in because Memory() requires a model provider at initialisation; documented in the example README. Limitation: The Soil side is real; the framework side is a labelled stand-in.
- **Letta** (reproducible-in-repo): Runnable example under [examples/letta](../examples/letta): the Soil side is real, the framework side is a clearly labeled stand-in because a Letta agent needs a server and a model provider; documented in the example README. Limitation: The Soil side is real; the framework side is a labelled stand-in.
- **buzz** (open-integration-observed): Live session on 2026-07-24: a real buzz-agent binary built from block/buzz, a real model choosing the tools, a save in one session and a load in a fresh process; [session report](../compatibility/reports/buzz-agent.md). Reproducible keyless example under [examples/buzz](../examples/buzz): the same driver runs the pinned buzz-agent with a scripted model stand-in in [the examples workflow](../.github/workflows/examples.yml). The buzz-acp harness itself was not run: it needs a running relay, and its single MCP slot is documented from source in the example README. Limitation: The run with a real model is a committed report, not an automated check; CI runs a stand-in.

## Operating systems

What continuous integration proves per operating system, from the CI matrix
on main. The portable core is pnpm build, the root vitest suite, the Python
tests, the Go tests, and the TypeScript, Python and Go conformance runners.

| Operating system | Suites green in CI                                                     | Last green |
| ---------------- | ---------------------------------------------------------------------- | ---------- |
| ubuntu-latest    | portable core, JVM suite and runner, .NET suite and runner, formatting | 2026-07-27 |
| macos-latest     | portable core                                                          | 2026-07-27 |
| windows-latest   | portable core                                                          | 2026-07-27 |

The JVM and .NET suites run on one operating system because their code is
pure managed runtime and they are the slowest suites in the repository.

Last generated from source data: 2026-07-31, the most recent evidence
date in the source files.
